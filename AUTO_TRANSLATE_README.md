# Автоматический перевод в TinaCMS

Система автоматического перевода контента с использованием OpenAI GPT-4o-mini.

## 🎯 Возможности

- **Directory-Based локализация**: контент организован по папкам `ru/` и `en/`
- **Кнопка перевода в редакторе**: перевод документов одним кликом
- **GitHub Action**: автоматический перевод при мерже PR
- **Индикатор автоперевода**: отмечает переведённые поля

## 📁 Структура контента

```
content/
├── pages/
│   ├── en/
│   │   ├── home.mdx
│   │   └── about.mdx
│   └── ru/
│       ├── home.mdx
│       └── about.mdx
├── posts/
│   ├── en/
│   │   └── my-post.mdx
│   └── ru/
│       └── my-post.mdx
└── services/
    ├── en/
    │   └── service.mdx
    └── ru/
        └── service.mdx
```

## 🔧 Настройка

### 1. OpenAI API Key

Добавьте API ключ в `.env.local`:

```bash
OPENAI_API_KEY=sk-...
```

### 2. GitHub Actions (опционально)

Для автоматического перевода через GitHub Actions добавьте секрет:

1. Перейдите в Settings → Secrets and variables → Actions
2. Добавьте `OPENAI_API_KEY` со значением вашего API ключа OpenAI

## 🚀 Использование

### Перевод через редактор TinaCMS

1. Откройте документ в TinaCMS (например, `/admin#/collections/page/ru/about`)
2. В toolbar редактора найдите кнопку **"Translate to EN/RU"** (🌐)
3. Нажмите кнопку - документ будет автоматически переведён
4. Система предложит открыть переведённый документ
5. Проверьте перевод и внесите правки при необходимости
6. Сохраните изменения

### Автоматический перевод через GitHub Action

Workflow автоматически запускается при:
- Мерже PR с изменениями в `content/**/en/**`

Процесс:
1. Мержите PR с изменениями в английском контенте
2. GitHub Action автоматически переводит файлы на русский
3. Создаётся новый PR с переводами
4. Проверьте переводы и мержите PR

### Ручной перевод через скрипт

```bash
# Установите переменную окружения
export OPENAI_API_KEY=sk-...

# Запустите скрипт
node scripts/auto-translate.mjs
```

## 🌐 Роутинг

### Английская версия (по умолчанию)
- `/` - главная страница
- `/about` - страница about
- `/posts/my-post` - пост
- `/services/service` - сервис

### Русская версия
- `/ru` - главная страница
- `/ru/about` - страница about
- `/posts/ru/my-post` - пост
- `/services/ru/service` - сервис

## 🏷️ Индикатор автоперевода

Переведённые поля отмечаются бейджем 🌐 **"Автоматический перевод"**.

При редактировании поля вручную индикатор автоматически удаляется.

## 📊 API Endpoints

### POST `/api/translate`
Переводит отдельный текст.

```typescript
{
  text: string,
  targetLocale: 'ru' | 'en',
  sourceLocale?: 'ru' | 'en'
}
```

### POST `/api/translate-document`
Переводит весь документ со всеми полями.

```typescript
{
  document: any,
  targetLocale: 'ru' | 'en',
  sourceLocale?: 'ru' | 'en',
  collection: string
}
```

## ⚙️ Технические детали

### Модель
- **OpenAI GPT-4o-mini** с температурой 0.3
- Сохраняет форматирование и структуру MDX/Markdown
- Переводит только текстовые поля, не трогает метаданные и код

### Хранение метаданных
- `_autoTranslatedFields`: массив имён переведённых полей в каждом блоке
- Используется для отображения индикаторов в UI

### GitHub Action
- Workflow: `.github/workflows/auto-translate.yml`
- Скрипт: `scripts/auto-translate.mjs`
- Создаёт отдельный PR для каждого перевода

## 🔍 Отладка

### Проверка конфигурации
```bash
# Проверьте наличие API ключа
echo $OPENAI_API_KEY

# Проверьте структуру контента
ls -la content/pages/ru/
ls -la content/pages/en/
```

### Логи GitHub Action
Посмотрите логи в разделе Actions вашего репозитория.

### Ошибки API
Проверьте консоль браузера или логи Next.js для деталей ошибок API.

## 📝 Примечания

- Автоперевод - это помощник, всегда проверяйте качество перевода
- Переведённые документы создаются с тем же именем файла в папке целевой локали
- Если файл уже существует, он НЕ будет перезаписан

## 🎨 Кастомизация

### Изменить промпт перевода
Отредактируйте промпты в:
- `app/api/translate/route.ts`
- `app/api/translate-document/route.ts`
- `scripts/auto-translate.mjs`

### Добавить новые коллекции
Добавьте их в `tina/plugins/auto-translate-action.tsx`:

```typescript
addTranslateAction(cms, {
  collections: ['page', 'post', 'service', 'your-collection'],
})
```

### Изменить модель
Замените `gpt-4o-mini` на другую модель OpenAI в API endpoints и скрипте.

