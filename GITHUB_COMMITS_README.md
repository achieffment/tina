# Автоматические Batch коммиты для переводов TinaCMS

## Проблема

При создании автоматических переводов на Vercel файлы создаются на эфемерной файловой системе и не попадают в git репозиторий.

## Решение

Используем **GitHub API (Octokit)** для создания batch коммитов - **один коммит для всех переведённых языков сразу**.

## Как это работает

```
1. Выбираете языки для перевода: de, fr, es, it, ja
   ↓
2. UI плагин переводит все языки параллельно
   ↓
3. Все переводы накапливаются в массив
   ↓
4. Отправляется один запрос к Batch API
   ↓
5. Все файлы сохраняются локально (немедленный доступ)
   ↓
6. Создаётся ОДИН batch коммит в GitHub
   ↓
7. Результат: "Add translations to de, fr, es, it, ja: pages/home.mdx"
```

## Результат

**До:** 5 языков = 5 отдельных коммитов  
**После:** 5 языков = 1 batch коммит ✨

## Архитектура

```
UI Plugin (auto-translate-action.tsx)
    ↓ Переводит все языки параллельно (батчами по 4)
    ↓ Накапливает переводы в массив
    ↓
Batch API (/api/create-translated-files-batch)
    ↓ Сохраняет все файлы локально
    ↓ Создаёт blobs для каждого файла
    ↓ Создаёт один tree со всеми изменениями
    ↓ Создаёт один commit
    ↓
GitHub Repository (один информативный коммит)
```

## Настройка переменных окружения

### Для локальной разработки (опционально)

Добавьте в `.env.local`:

```env
GITHUB_OWNER=your-username
GITHUB_REPO=your-repo-name
GITHUB_BRANCH=main
GITHUB_TOKEN=ghp_xxxxxxxxxxxxx
```

> **Примечание**: Локально это опционально. Без GitHub credentials файлы создаются локально, коммит не делается.

### Для Vercel (обязательно для автоматических коммитов)

Добавьте в **Vercel Environment Variables**:

| Variable | Example | Description |
|----------|---------|-------------|
| `GITHUB_OWNER` | `achieffment` | GitHub username или организация |
| `GITHUB_REPO` | `tina` | Название репозитория |
| `GITHUB_BRANCH` | `main` | Ветка для коммитов |
| `GITHUB_TOKEN` | `ghp_xxx...` | Personal Access Token |

## Создание GitHub Personal Access Token

1. Перейдите: https://github.com/settings/tokens
2. Нажмите **Generate new token (classic)**
3. Дайте имя: `TinaCMS Auto-Translate`
4. Выберите срок действия (рекомендуем: No expiration)
5. Выберите права:
   - ✅ `repo` (Full control of private repositories)
6. Нажмите **Generate token**
7. Скопируйте токен (он показывается только один раз!)

## Добавление переменных в Vercel

1. Откройте ваш проект на https://vercel.com
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте каждую переменную:
   - Name: `GITHUB_OWNER`
   - Value: ваш username
   - Environment: **Production, Preview, Development** (выбрать все)
4. Повторите для всех 4 переменных
5. Нажмите **Save**
6. **Redeploy** проект для применения изменений

## Пример использования

### 1. Локально

```bash
# Запустить dev сервер
pnpm dev

# Открыть TinaCMS admin
open http://localhost:3000/admin

# Открыть любую страницу, например pages/en/home.mdx
# Нажать кнопку "Auto-translate"
# Выбрать несколько языков: de, fr, es
# Нажать "Translate selected languages"
```

### 2. Результат

```bash
# Проверить созданные файлы
ls -la content/pages/de/home.mdx
ls -la content/pages/fr/home.mdx
ls -la content/pages/es/home.mdx

# Проверить коммит
git log --oneline -1
# Output: "Add translations to de, fr, es: pages/home.mdx"

# Посмотреть все файлы в коммите
git show --stat
# Output:
# content/pages/de/home.mdx | 240 ++++++++++++++++++
# content/pages/fr/home.mdx | 240 ++++++++++++++++++
# content/pages/es/home.mdx | 240 ++++++++++++++++++
# 3 files changed, 720 insertions(+)
```

## Режимы работы

### 1. Batch GitHub + Local Mode (Рекомендуемый)

✅ Переводит все языки параллельно  
✅ Сохраняет все файлы локально (немедленный доступ)  
✅ Создаёт **один batch коммит** для всех языков через GitHub API  
✅ Информативное сообщение: `Add translations to de, fr, es: pages/home.mdx`

### 2. Local Mode (Fallback)

⚠️ Только локальное сохранение файлов  
⚠️ Используется если GitHub credentials не настроены  
⚠️ Требует ручного `git add` и `git commit` позже

## Формат commit message

```
Add translations to {locale1, locale2, locale3}: {collection}/{relativePath}
```

**Примеры:**
- `Add translations to de, fr, es: pages/home.mdx`
- `Add translations to ja, ko, zh: posts/learning-about-tinacms.mdx`
- `Add translations to ru, pl, pt: services/retouch.mdx`

## Компоненты системы

### 1. Batch API Route
`/app/api/create-translated-files-batch/route.ts`

```typescript
interface BatchCreateRequest {
  files: TranslatedFile[];
  sourceLocale: string;
  sourceDocumentPath: string;
}

// Принимает массив файлов
// Сохраняет локально
// Создаёт batch коммит через Octokit
```

### 2. UI Plugin
`/tina/plugins/auto-translate-action.tsx`

- Переводит языки параллельно (батчами по 4)
- Накапливает все переводы
- Отправляет один batch запрос

### 3. Зависимости

```json
{
  "@octokit/rest": "^22.0.1",
  "gray-matter": "^4.0.3"
}
```

## Отладка

### Локальная разработка

Логи в консоли браузера и терминале:

```
[TRANSLATE:UI] 📦 БАТЧ 1/2: 4 языков параллельно
[TRANSLATE:UI] Языки: Deutsch, Français, Español, Italiano
[TRANSLATE:UI] ✅ Батч 1 завершен за 1234ms

[TRANSLATE:BATCH] ==========================================
[TRANSLATE:BATCH] Начало batch создания файлов
[TRANSLATE:BATCH] Параметры: { filesCount: 4, locales: 'de, fr, es, it', ... }
[TRANSLATE:BATCH] ✓ Все файлы сохранены локально за 45ms
[TRANSLATE:BATCH] ✓ Batch коммит создан в GitHub за 892ms
[TRANSLATE:BATCH] ==========================================
```

### Vercel

Проверьте логи:
1. Перейдите в **Deployments**
2. Выберите последний деплой
3. Откройте **Functions**
4. Найдите логи с `[TRANSLATE:BATCH]`

## Troubleshooting

### ❌ Ошибка: "GitHub credentials not configured"

**Решение:** Добавьте переменные окружения в Vercel

### ❌ Ошибка: "Bad credentials" или 401

**Решение:**
- Проверьте, что токен скопирован правильно (без пробелов)
- Убедитесь, что токен имеет права `repo`
- Создайте новый токен

### ❌ Ошибка: "Not Found" или 404

**Решение:**
- Проверьте `GITHUB_OWNER` и `GITHUB_REPO`
- Убедитесь, что токен имеет доступ к репозиторию
- Для приватных репозиториев обязательны права `repo`

### ⚠️ Файлы создаются локально, но нет коммита

**Решение:** 
- GitHub credentials не настроены (это fallback режим)
- Добавьте переменные в `.env.local` для локального тестирования коммитов

### ⚠️ Переводы работают, но коммит не появляется в GitHub

**Решение:**
- Проверьте переменные окружения в Vercel
- Redeploy проект после добавления переменных
- Проверьте логи Functions в Vercel

## Преимущества batch коммитов

✅ **Чистая история git**: Один коммит вместо множества  
✅ **Информативные сообщения**: Видно сразу все переведённые языки  
✅ **Атомарность**: Все переводы коммитятся вместе или не коммитятся вовсе  
✅ **Производительность**: Меньше API запросов к GitHub  
✅ **Простой rollback**: Откатить один коммит проще, чем несколько

## GitHub API операции

Под капотом происходит:

1. `octokit.git.getRef()` - получение текущего SHA ветки
2. `octokit.git.getCommit()` - получение текущего tree SHA
3. `octokit.git.createBlob()` - создание blob для каждого файла
4. `octokit.git.createTree()` - создание tree со всеми файлами
5. `octokit.git.createCommit()` - создание коммита
6. `octokit.git.updateRef()` - обновление ветки

Всё это в **одной транзакции** → один batch коммит! 🎯

## Резюме

✅ **Batch коммиты**: Один коммит для всех языков  
✅ **Информативные сообщения**: `Add translations to de, fr, es: pages/home.mdx`  
✅ **Локально**: Файлы сразу + batch коммит в GitHub  
✅ **Vercel**: Автоматический batch коммит через API  
✅ **Немедленный результат**: Файлы доступны сразу  
✅ **Автоматизация**: Не нужно коммитить вручную  
✅ **Fallback**: Работает даже без GitHub credentials (только локально)
